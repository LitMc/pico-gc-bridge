[project]
name = "pico-gc-bridge"
version = "0.1.0"
description = "GC controller correction bridge for Raspberry Pi Pico"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "opencv-python",
    "numpy",
]
